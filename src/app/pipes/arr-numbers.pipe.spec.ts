import { ArrNumbersPipe } from './arr-numbers.pipe';

describe('ArrNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
